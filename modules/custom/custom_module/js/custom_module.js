(($, Drupal) => {
  'use strict';

  const $win = $(window);
  const $doc = $(document);

  /*
   * Drupal behaviors
   */
  Drupal.behaviors.custom_module = {
    attach: (context, settings) => {
      
    }
  };

})(jQuery, Drupal);